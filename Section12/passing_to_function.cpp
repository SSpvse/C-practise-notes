#include <iostream>
#include <string>
#include <iomanip>


using namespace std;

using std::cin;
using std::cout;


int main() {


    // in this one we learn that, pointer pointing to const int a ... cant change the value of a but can change where it points to , maybe to int b ...
    // if we have int *const score_ptr {&a}; means it points to a and cannot change to point to b... however, a can change its value ...

    // so const pointers cant change where they point to ... but the value of variable they are pointing to , the varialbes value can change..



    return 0;
}

